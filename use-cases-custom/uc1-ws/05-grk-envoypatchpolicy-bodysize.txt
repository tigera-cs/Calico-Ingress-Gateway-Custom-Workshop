apiVersion: gateway.envoyproxy.io/v1alpha1
kind: EnvoyPatchPolicy
metadata:
  name: large-body-size-policy
  namespace: default
spec:
  targetRef:
    group: gateway.networking.k8s.io
    kind: Gateway
    name: grk-keepalive-timeout-gateway
  type: JSONPatch
  jsonPatches:
  - type: type.googleapis.com/envoy.config.listener.v3.Listener
    name: "default/grk-keepalive-timeout-gateway/http"   # Important: listener name format
    operation:
      op: replace
      path: "/default_filter_chains/filters/0/typed_config/max_request_bytes"
      value: 524288000   # 500 MiB (safe upper limit for demo)
